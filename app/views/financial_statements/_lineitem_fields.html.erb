  	<fieldset>
  		<%= f.label :account, "Account" %>
  		<%= collection_select(f, :account_id, Account.all, :id, :name) %>
  		<%= f.label :amount, "Amount" %>
  		<%= f.text_field :amount %>
  		<%= f.hidden_field :_destroy %>
  		<%= link_to 'remove', '#', class: "remove_fields" %>
	</fieldset>