  	<fieldset class="well form-inline">
<!--	  		<%= f.label :account, "Account", :class => "control-label" %>-->
				<%= f.grouped_collection_select(:account_id, Section.where(:statement_type => "0"), :accounts, :name, :id, :name) %>
	  		<%= f.label :amount, "Amount:", :class => "control-label" %>

	  		<%= f.text_field :amount %>
	  		<%= f.hidden_field :_destroy %>
	  		<a class="btn btn-danger remove_fields" href="#"><i class="icon-trash icon-white"></i> Delete</a>
   	        <p class="help-block">Supporting help text</p>

	  		<!--
			<%= link_to 'New Account', new_account_path, target: "blank" %>
			-->
	</fieldset>